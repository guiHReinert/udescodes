#include <stdio.h>
#include <string.h>

char fruta[9];
int i, calorias, quant,
banana, maca, mamao, tangerina;

int main()
{
  printf("Programa de auxílio nutrocional.\nInforme as frutas que consumiu hoje.");
    for(i = 0, i <= 2, i++){
      if(){
        printf("\nDigite a primeira fruta: ");
      };
      else if(){
        printf("\nDigite a segunda fruta: ");
      };
      else{
        printf("\nDigite a terceira fruta: ");
      }
      scanf{"%s", fruta};
      printf("\n Qual a quantidade?");
      scanf("%s", quant);
      
      switch(fruta){
        case "banana":
        banana += 1;
        total += quant * 89;
        break;
        case "maca":
        maca += 1;
        total += quant * 55;
        break;
        case "mamao":
        mamao += 1;
        total += quant * 108;
        break;
        case "tangerina":
        tangerina += 1;
        total += tangerina * 40;
        break;
        }
      }
      printf("\nTotal de calorias:");
      
    }

    return 0;
}
